suite: marketPlace/eks dispatch
# NOTE: The chart references kinetica-operators.eks-dboperator-operator which
# has no corresponding template file yet. Tests disable dbOperator to avoid
# that missing include and verify the resources that ARE testable.
values:
  - __base_values.yaml

tests:
  - it: renders for marketPlace/eks with dbOperator disabled
    template: kinetica_operators.yaml
    set:
      environment: "marketPlace"
      provider: "eks"
      dbOperator.install: false
    asserts:
      - notFailedTemplate: {}

  - it: creates WB operator Deployment for marketPlace/eks
    template: kinetica_operators.yaml
    set:
      environment: "marketPlace"
      provider: "eks"
      dbOperator.install: false
    asserts:
      - containsDocument:
          kind: Deployment
          apiVersion: apps/v1
          name: workbench-operator-controller-manager
          any: true

  - it: creates namespaces via wb-only path
    template: kinetica_operators.yaml
    set:
      environment: "marketPlace"
      provider: "eks"
      dbOperator.install: false
    asserts:
      - containsDocument:
          kind: Namespace
          apiVersion: v1
          name: gpudb
          any: true
